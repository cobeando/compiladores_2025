package ar.edu.unnoba.comp.compilertp;

import guru.nidi.graphviz.engine.Format;
import guru.nidi.graphviz.engine.Graphviz;
import guru.nidi.graphviz.model.Graph;
import guru.nidi.graphviz.parse.Parser;

import java.io.File;
import java.io.IOException;

public class GraphGenerator {
    public void generateGraph(String dotString, String outputFilePath) throws IOException {
//        Graph graph = Parser.read(dotString);
//        Graphviz.fromGraph(graph).render(Format.PNG).toFile(new File(outputFilePath));
    }
}

